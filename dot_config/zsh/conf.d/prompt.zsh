# ------------------------------------------------------------------------------
# Prompt
#
# This file configures the shell prompt and directory navigation.
# ------------------------------------------------------------------------------

eval "$(starship init zsh)"

eval "$(zoxide init zsh)"
